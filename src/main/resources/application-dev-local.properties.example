# Copy file này thành: application-dev-local.properties (cùng thư mục)
# File thật sẽ bị .gitignore bỏ qua để tránh commit secrets.

# --- Database (MySQL local) ---
# Nếu bạn dùng root + password, chỉ cần set đúng password ở đây.
# spring.datasource.url=jdbc:mysql://localhost:3306/logistic?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Ho_Chi_Minh
# spring.datasource.username=root
# spring.datasource.password=YOUR_MYSQL_PASSWORD

# --- JWT (nên set để token không bị đổi sau restart) ---
# jwt.secret=PUT_A_LONG_RANDOM_SECRET_HERE
# jwt.expiration=86400000

# --- VNPAY (nếu cần test) ---
# vnpay.return-url=http://localhost:9090/payment/vnpay-return
# vnpay.tmn-code=YOUR_TMN_CODE
# vnpay.secret-key=YOUR_SECRET_KEY
